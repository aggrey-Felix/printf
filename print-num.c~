#include "main.h"

/**
 * print_i - prints an integer
 * @i: integer to print
 * @d: integer to print
 * Return: number of chars and digits printed
 */
int print_i(va_list i)
{
  {
   int data = 65;
   printf("%i\n", data);
   printf("%d\n", data);
   return (0);
}
